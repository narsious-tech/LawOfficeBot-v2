# Sprint 10.3 Empty-State Fix

This build detects a zero-hearing day from the normalized Advocate Diaries court groups and case arrays instead of relying only on a numeric count.

Expected dashboard marker:

`🧩 Build: Sprint 10.3 Empty-State Fix`

When no cases are returned, the dashboard suppresses Court Movement Plan, Suggested Staff Deployment, and Floor-wise Cause List and shows one concise court-schedule message.
