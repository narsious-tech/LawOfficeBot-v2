# Pack 1 — Core Dependencies and Find Case

Upload these paths to the repository root:

- `advocate_web.py`
- `utils/__init__.py`
- `utils/drive.py`
- `commands/find_case.py`

Replace existing files where applicable.

Commit message:

`Pack 1: add production dependencies and find-case command`

Do not change handler registration yet. A separate integration pack will switch
the new-case and find-case handlers safely.
